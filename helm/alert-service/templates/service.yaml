apiVersion: v1
kind: Service
metadata:
  name: alert-service
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: alert-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
